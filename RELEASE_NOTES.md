# ShizuLog v1.8.0

## 日志分析工作台

v1.8.0 在 v1.7.0 专业日志查看器基础上加入持久化分析能力。

### 整份日志过滤

支持组合过滤：

- 最低日志等级：全部 / D+ / I+ / W+ / E+
- Tag 包含
- PID 精确匹配
- 进程 / 包名关键字
- 日志正文关键字
- 仅崩溃标记

过滤会扫描整份 `.log`，不是只处理当前 256 KB 页面。最多保存 5000 条结果索引，避免极端大日志占用过多内存。

### 过滤导航

筛选后支持：

- 筛上个 / 筛下个
- 自动跳到对应分页
- 自动滚动到目标行
- 高亮目标行
- 显示精确行号

### 过滤预设

内置：

- 全部日志
- 警告及以上
- 错误及以上
- 崩溃标记
- 当前目标包（当 ShizuLog 已选择目标 App 时）

还可以把当前等级、Tag、PID、进程/包名、正文关键字和崩溃开关保存成自定义预设；同名保存会覆盖旧预设。

### 持久化日志书签

搜索结果、ERROR、过滤结果或当前页位置都可以收藏。

书签按日志文件分别保存：

- 字节位置
- 精确行号
- 日志预览

关闭 ShizuLog 后重新打开同一份历史日志，书签仍然存在。

支持：

- 收藏当前位置
- 取消当前位置收藏
- 查看当前日志全部书签
- 点击书签跳回原位置并高亮
- 清空当前日志全部书签

### 继续保留 v1.7.0 功能

- 整份关键词搜索
- 正则搜索
- 上一个 / 下一个匹配
- ERROR 索引
- 行号
- 复制当前位置上下文
- 字号调节
- 自动换行
- 256 KB 分页
- 正在写入日志刷新

## 固定签名

v1.8.0 继续使用 v1.5.2 建立的固定正式签名。

升级脚本不会替换签名工作流，只更新现有 Release tag。检测不到以下任一项时会直接停止：

- `SHIZULOG_KEYSTORE_B64`
- `assembleRelease`
- `apksigner`
