# ShizuLog v1.9.0

## 实时 Logcat 分析

v1.9.0 将分析能力前移到录制过程中。

### 最近 60 秒实时统计

首页录制状态区域新增“实时分析”卡片，持续显示：

- V / D / I / W / E / F 等级数量
- 最近 60 秒日志速率（行/s）
- ERROR 速率（E+F / min）
- Top 3 Tag
- 最近窗口出现的 PID 数
- 从本次录制开始累计的 PID / 进程变化次数
- 最近一次进程启动、退出或结束事件

实时统计只保留有限滚动事件窗口，不会无限增长占用内存。

### 实时预览过滤

首页日志预览由原来的：

- 全部
- WARN+
- ERROR

扩展为：

- 全部
- D+
- I+
- W+
- E+

并新增：

- Tag 包含过滤
- PID 精确过滤
- 进程 / 包名关键字过滤
- 原有正文搜索继续保留

这些条件可以组合使用。

### 过滤不影响落盘日志

实时过滤仅控制首页 TextView 里“看见什么”。

`LogCaptureService` 写入磁盘的日志仍保持完整，不会因为：

- 选择 E+
- 输入 Tag
- 输入 PID
- 输入包名
- 搜索关键词

而丢弃原始记录。

完整日志、历史日志、诊断包和 v1.8.0 工作台仍然读取完整落盘日志。

### 进程变化识别

实时分析会识别常见 Android 系统进程生命周期文本，例如：

- `Start proc ...`
- `Process ... (pid ...) has died`
- `Killing pid:process`

同时也会根据 threadtime,uid 日志中首次出现的新 PID 记录变化。

## 与 v1.8.0 兼容

保留此前：

- 专业完整日志查看器
- 整份搜索 / 正则
- ERROR 索引
- Tag / PID / 进程组合过滤
- 自定义过滤预设
- 持久化日志书签
- 隐私诊断包
- 崩溃分析
- 全局日志轮转

## 固定签名

v1.9.0 继续使用 v1.5.2 建立的固定正式签名。

升级脚本要求当前项目已经包含 v1.8.0 的 `LogFilterEngine.java`，并继续检查：

- `SHIZULOG_KEYSTORE_B64`
- `assembleRelease`
- `apksigner`

不会生成或替换签名密钥。
