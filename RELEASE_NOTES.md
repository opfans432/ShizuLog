# ShizuLog v1.5.1

## 崩溃分析器

新增本地崩溃分析页面，直接读取 ShizuLog 已保存的 `.log`，无需上传日志、无需联网。

支持识别：

- Java / Kotlin `FATAL EXCEPTION`
- `ANR in ...`
- Native `Fatal signal`
- `SIGSEGV / signal 11`
- `SIGABRT / signal 6`
- 常见 native crash 标记

## 自动提取

检测到崩溃后尝试整理：

- 崩溃类型
- 进程
- PID
- 线程
- 异常类 / Native signal
- 根因（`Caused by`）
- 关键调用位置
- 最多约 140 行堆栈片段

Java 崩溃会优先选择第一个非 Android / Java / Kotlin 框架调用栈作为“关键位置”。

## 入口

### 当前日志

首页“当前状态”区域现在提供：

- 完整日志
- 崩溃分析
- 历史日志

只要当前日志文件存在，就可以直接进入分析。

### 历史日志

历史日志中被标记为“检测到崩溃”的记录会出现：

- 分析
- 导出
- 删除

点击“分析”直接进入该日志的崩溃分析页。

## 分析页面

支持：

- 复制崩溃摘要
- 查看提取后的堆栈片段
- 长按选择堆栈文字
- 一键进入完整原始日志

## 性能与隐私

- 分析在后台线程执行
- 默认只读取日志尾部最多约 4 MB
- 不把日志上传到服务器
- 无明显崩溃标记时会明确显示“未检测到明显崩溃”

> 崩溃分析是基于日志文本的自动提取，最终结论仍应结合完整日志、源码和复现条件判断。
