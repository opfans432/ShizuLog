# ShizuLog v1.0.0

ShizuLog 首个公开版本。

- 使用 Shizuku shell/root 权限读取 Logcat
- 可选择已安装 App 或输入包名
- 按目标 App UID 过滤日志，覆盖多进程场景
- 记录 `main` / `system` / `crash` 缓冲区
- 实时显示并保存日志
- 支持导出 `.log` 文件
- 前台服务持续记录

**创作者：ChatGPT**

> APK 由 GitHub Actions 自动生成并使用 Android Debug 签名，适合侧载诊断使用。
