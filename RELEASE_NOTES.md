# ShizuLog v1.7.0

## 专业日志查看器

v1.7.0 重点升级“完整日志”页面。

### 整份日志搜索

不再只搜索当前 256 KB 页面。

现在可以扫描整个 `.log`：

- 普通关键词搜索
- 正则表达式搜索
- 上一个 / 下一个匹配
- 显示当前匹配序号
- 显示精确日志行号
- 搜索结果上限 5000 条，防止极端日志占用过多内存

搜索在后台线程执行，不会一次性把整份日志加载到内存。

### ERROR 索引

新增：

```text
跳 ERROR
```

会扫描整份日志，建立常见错误/崩溃索引，包括：

- Logcat `E`
- `FATAL EXCEPTION`
- ANR
- Fatal signal
- SIGSEGV / signal 11
- SIGABRT / signal 6
- Native crash

再次点击会从当前位置跳向下一个 ERROR；到文件末尾后从第一个重新开始。

### 行号

完整日志每一行现在显示固定宽度行号：

```text
   12842 | 08-12 12:30:21.441 ...
   12843 | 08-12 12:30:21.457 ...
```

页面栏同时显示当前页覆盖的行号范围。

### 匹配高亮

跳转到搜索结果或 ERROR 后：

- 自动切换到对应分页
- 自动滚动到目标行
- 高亮当前目标行

### 复制错误块

搜索到问题后可点击：

```text
复制错误块
```

ShizuLog 会读取该匹配位置前约 8 行、后约 24 行，并带行号复制到剪贴板，适合直接发给开发者分析。

### 阅读控制

新增：

- `A−`
- `A+`
- 自动换行开 / 关
- 到底部
- 刷新
- 第一页 / 上一页 / 下一页 / 最后一页

字号范围约 9sp–22sp。

关闭自动换行后可以横向查看超长 Logcat 行。

### 大日志设计

仍然使用约 256 KB 分页，不会为了显示完整日志而一次性把几十 MB 或几百 MB 文件放进 TextView。

整份搜索和 ERROR 索引通过流式扫描文件完成。

## 固定签名

v1.7.0 继续使用 v1.5.2 建立的固定正式签名。

升级脚本不会替换 GitHub Actions，只会把现有固定签名工作流中的 Release tag 从旧版本更新为 v1.7.0。

如果脚本检测不到：

- `SHIZULOG_KEYSTORE_B64`
- `assembleRelease`
- `apksigner`

会停止更新，避免重新产生签名不一致 APK。
