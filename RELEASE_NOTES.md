# ShizuLog v1.2.0

v1.2.0 开始增强日志查看与记录过程的交互体验。

## 日志筛选

- 新增日志搜索框
- 支持实时搜索当前日志内容
- 新增 `全部` / `WARN+` / `ERROR` 快速筛选
- ERROR 模式会优先识别 E/F 级日志、FATAL EXCEPTION、Caused by、AndroidRuntime 等崩溃线索
- 显示当前匹配行数与总行数
- 无匹配结果时显示明确空状态

## 性能

- 日志 UI 改为约 80ms 批量刷新
- 避免每收到一行 Logcat 就立即重绘整个 TextView
- 保持完整日志持续写入文件，筛选仅影响屏幕预览

## 记录状态

- 开始 / 停止按钮根据记录状态自动启用或禁用
- 未选择目标 App 时禁止开始记录和打开目标 App
- 正在记录时“开始记录”自动变为“正在记录”
- 新增记录状态 Chip
- 没有日志文件时禁止导出
- 没有目标 App 时禁止手动崩溃快照

## 同时保留

- 可搜索目标 App Picker
- App Picker 后台加载与缓存
- 防止重复弹出多个 App Picker
- 手动包名折叠
- 日志空状态
- 崩溃日志恢复与补抓
