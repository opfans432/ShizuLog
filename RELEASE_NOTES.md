# ShizuLog v1.1.1

这是针对 v1.1.0 的交互与稳定性修复版本。

## 修复

- 修复“选择目标应用”首次打开时明显卡顿的问题
- 修复快速连续点击“选择目标应用”会叠加多个选择窗口的问题
- App 扫描移到后台线程，不再阻塞主界面
- 使用一次 Launcher 查询替代逐个应用检查启动入口
- App 列表加入内存缓存，重复打开更快
- App 图标改为按需异步加载

## 新增

- 可搜索的目标应用选择器
- 支持按应用名称搜索
- 支持按包名搜索
- 实时显示匹配结果数量
- 无结果时显示明确空状态

此版本不改变日志采集核心逻辑。
