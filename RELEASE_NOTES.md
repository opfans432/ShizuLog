# ShizuLog v2.0.0

## 主版本：会话化日志工作台

v2.0.0 不再把所有入口堆在同一个记录页面。启动应用后首先进入新的 Material 3 工作区首页。

首页明确分为：

- 实时日志
- 记录会话
- 历史日志
- 分析工作台
- 崩溃分析

最近一次会话会直接显示在首页。

## 记录会话

从 v2.0.0 开始，每次点击“开始记录”都会创建一个独立会话。

会话自动保存：

- 会话 ID
- 自动名称
- 单应用 / 多应用 / 全局模式
- 目标应用包名
- 目标应用显示名称
- UID
- 开始时间
- 结束时间
- 当前/最终状态
- Logcat 文件路径
- 日志文件大小

记录结束后，会话仍然可以重新打开。

## 会话详情

每个会话可以直接进入：

- 专业日志分析工作台
- 崩溃分析
- 隐私诊断包

还支持：

- 重命名会话
- 删除会话索引

“删除会话索引”不会删除原始 `.log` 文件，避免误删排错证据。

## 自动恢复

如果 ShizuLog 上次退出时存在尚未结束的会话，但应用状态已经显示不再记录，v2.0.0 会在下次启动记录页时自动把该会话标记为结束，避免永久显示为“正在记录”。

## 首页快捷分析

“分析工作台”和“崩溃分析”会列出最近最多 20 个具有有效日志文件的会话，选择后直接进入对应工具。

## 兼容现有能力

v2.0.0 保留此前已经完成的：

- 单应用 / 多应用 / 全局 Logcat
- 动态 PID 与多进程记录
- 全局日志轮转
- 实时 Logcat 分析
- 实时 Tag / PID / 进程过滤
- 历史日志管理
- 崩溃分析
- 诊断包与隐私脱敏
- 整份日志搜索 / 正则
- Tag / PID / 进程 / 等级过滤
- 自定义过滤预设
- 日志书签

## 固定签名

继续使用 v1.5.2 建立的固定正式签名。

升级脚本不会生成或替换签名密钥。检测不到：

- `SHIZULOG_KEYSTORE_B64`
- `assembleRelease`
- `apksigner`

时会停止升级。
